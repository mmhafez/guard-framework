# OS / editor noise
.DS_Store
__MACOSX/
Thumbs.db
*.swp
.idea/
.vscode/

# Python cache (if anyone runs tooling)
__pycache__/
*.pyc

# Local env / secrets — GUARD ships none; keep it that way
.env
*.local
credentials.json
*.pem
*.key

# Build artifacts (produced by build-skill.sh / generate_figures.py)
/guard-framework.skill
.skill-build/
docs/figures/*.png
